# Sample .env for scripts/bob-api-tester.sh
# Copy to .env and edit values, or set BOB_ENV_FILE to point to this file.

# Cloud Functions region
REGION=europe-west2

# Firebase project ID
PROJECT=your-project-id

# Authenticated Firebase user context
OWNER_UID=your-firebase-auth-uid
ID_TOKEN=eyJhbGciOiJSUzI1NiIsImtpZCI6I...  # paste a fresh token from your app

# Optionally acquire ID_TOKEN automatically (email/password flow)
FIREBASE_WEB_API_KEY=your-web-api-key
AUTH_EMAIL=dev@example.com
AUTH_PASSWORD=yourpassword

# Optional webhook secrets (if configured)
N8N_WEBHOOK_SECRET=your-n8n-secret
REMINDERS_WEBHOOK_SECRET=your-reminders-secret

# You can also place GOOGLEAISTUDIOAPIKEY / NYLAS_API_KEY here if you want them available
# to the environment, but they are not required by the test script itself.
# GOOGLEAISTUDIOAPIKEY=
# NYLAS_API_KEY=
